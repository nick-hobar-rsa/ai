// When the agent does something wrong in your day-to-day coding, you type /flag "Tried to allocate memory dynamically".
// The script grabs the last 5 turns from history.json and saves it as traces/trace_TIMESTAMP.json
